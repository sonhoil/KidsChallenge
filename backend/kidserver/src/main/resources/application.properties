spring.application.name=kidspoint-server


#spring.datasource.url=jdbc:postgresql://localhost:5432/postgres?currentSchema=kidspoint
spring.datasource.url=jdbc:postgresql://yamanote.proxy.rlwy.net:57341/postgres?currentSchema=kidspoint

spring.datasource.username=postgres
#spring.datasource.password=ggthegame2@
spring.datasource.password=uhooUZSfxJXIuGufxNzkFNCqkpZogCES
spring.datasource.driver-class-name=org.postgresql.Driver


spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# Spring Session JDBC
spring.session.store-type=jdbc
spring.session.jdbc.initialize-schema=never
spring.session.timeout=30m
# 세션 쿠키 설정 (크로스 도메인 리다이렉트 지원)
server.servlet.session.cookie.name=SESSION
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false
server.servlet.session.cookie.same-site=lax
server.servlet.session.cookie.domain=


server.port=${PORT:8080}
server.address=0.0.0.0

# 정적 리소스 설정 - /api/** 경로는 정적 리소스로 처리하지 않음
spring.web.resources.add-mappings=false


mybatis.mapper-locations=classpath:mapper/**/*.xml
mybatis.type-aliases-package=com.kidspoint.api
mybatis.configuration.map-underscore-to-camel-case=true
mybatis.configuration.default-fetch-size=100
mybatis.configuration.default-statement-timeout=30


# 로깅 레벨 설정 (개발 환경)
logging.level.com.kidspoint=DEBUG
logging.level.com.kidspoint.api.auth=DEBUG
logging.level.com.kidspoint.api.kids=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.security.web=DEBUG
logging.level.org.springframework.security.web.FilterChainProxy=DEBUG
logging.level.org.mybatis=DEBUG
logging.level.org.apache.ibatis=DEBUG
logging.level.org.springframework.jdbc.core=DEBUG
logging.level.org.springframework.jdbc.core.StatementCreatorUtils=TRACE

# 카카오 로그인 설정
# REST API 키 (백엔드에서 사용)
kakao.client-id=${KAKAO_CLIENT_ID:ec0330129144c677f684395b1907ca19}
# Client Secret (카카오 개발자 콘솔 > 제품 설정 > 카카오 로그인 > 보안에서 확인)
kakao.client-secret=${KAKAO_CLIENT_SECRET:g1XaWxpKAOORaEBBi1XK2uVRo2ZTAjfk}
# 카카오 로그인 콜백 URI (백엔드)
kakao.redirect-uri=${KAKAO_REDIRECT_URI:http://localhost:8080/api/auth/kakao/callback}
# 프론트엔드 리다이렉트 URI (카카오 로그인 후 이동할 프론트엔드 URL)
kakao.frontend-redirect-uri=${KAKAO_FRONTEND_REDIRECT_URI:http://localhost:5173}
# 네이티브 앱 키 (Flutter 앱에서 사용, 참고용)
kakao.native-app-key=01d96aed07dfdb284de6448247a0b8ae

# 구글 로그인 설정
# OAuth 2.0 Client ID (Google Cloud Console에서 생성)
google.client-id=${GOOGLE_CLIENT_ID:}
# OAuth 2.0 Client Secret (Google Cloud Console에서 생성)
google.client-secret=${GOOGLE_CLIENT_SECRET:}
# 구글 로그인 리다이렉트 URI (백엔드)
google.redirect-uri=${GOOGLE_REDIRECT_URI:http://localhost:8080/api/auth/google/callback}
# 프론트엔드 리다이렉트 URI
google.frontend-redirect-uri=${GOOGLE_FRONTEND_REDIRECT_URI:http://localhost:5173}

# 프론트엔드 URL 설정 (초대 링크 생성에 사용)
app.frontend-url=${APP_FRONTEND_URL:http://localhost:5173}

# 파일 업로드 설정
# Railway에서는 /data 경로에 볼륨을 마운트하여 영구 저장소 사용
# 로컬에서는 uploads/items 사용
app.upload.dir=${UPLOAD_DIR:/data/uploads/items}
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
