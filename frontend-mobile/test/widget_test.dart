// Smoke test — real app boots with async init in main(); integration tests cover flows.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
