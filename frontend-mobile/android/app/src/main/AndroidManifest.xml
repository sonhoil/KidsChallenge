<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    
    <!-- Android 11 이상에서 카카오톡 앱 감지를 위한 queries 선언 -->
    <queries>
        <package android:name="com.kakao.talk" />
    </queries>
    
    <application
        android:label="칭찬통장"
        android:icon="@mipmap/ic_launcher"
        android:name="${applicationName}"
        android:usesCleartextTraffic="true">
        <activity
            android:name="com.kidspoint.kids_challenge.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="kidspoint"
                    android:host="app"
                    android:pathPrefix="/login" />
            </intent-filter>
        </activity>
        
        <!-- 카카오 로그인을 위한 Intent Filter -->
        <activity
            android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- 카카오 네이티브 앱 키: 01d96aed07dfdb284de6448247a0b8ae -->
                <data android:scheme="kakao01d96aed07dfdb284de6448247a0b8ae" />
            </intent-filter>
        </activity>
        
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
